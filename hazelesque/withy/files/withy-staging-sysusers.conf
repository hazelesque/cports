# Staging-instance system user for withy-staging-{server,web}
# dinit services.  Separate UID from withy-demo so a compromise
# of one instance can't read the other's DB at the filesystem
# level.
u _withy-staging - "withy (staging instance)" /srv/data/staging/withy
