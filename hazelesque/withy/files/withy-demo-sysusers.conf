# Demo-instance system user for withy-demo-{server,web} dinit
# services.  Separate UID from withy-staging so a compromise of
# one instance can't read the other's DB at the filesystem
# level.
u _withy-demo - "withy (demo instance)" /srv/data/demo/withy
